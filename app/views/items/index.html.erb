<h2>All Menu Items</h2>

<div class="index">
<ul class="row">

  <% @items.each do |item| %>
    <li><h3><%= item.image.url(:thumb) %></h3>
    <h4><%= link_to "#{item.title}", item_path(item) %></h4></li>
  <% end %>
</ul>
</div>
