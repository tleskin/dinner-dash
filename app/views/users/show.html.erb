
<h1><%= "#{current_user.username} Dashboard" %></h1>

<%= image_tag current_user.picture.url(:thumb) %>
<%= button_to "Edit Profile", edit_user_path, method: :get %>
<%= button_to "Delete", user_path, method: :delete %>

<%= button_to "Orders", user_orders_path(user_id: current_user.id), method: :get %>
